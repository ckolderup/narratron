<div class="entry-text">
  <%= @entry.text %>
</div>

<hr class="faded">

<div class="guidance">
  Continue the story with one new sentence.
  <% if @entry.story.wrapping? %>
    This is the final sentence in this story path!
    Try to wrap it up as best you can.
  <% end %>
</div>

<% if current_user then %>
    <div class="entry-submission">
      <%= render 'form' %>
    </div>
<% else %>
  <div class="call-to-login">
    <%= link_to "Sign up", sign_up_path %> or
    <%= link_to "log in", log_in_path %> first!
  </div>
<% end %>
